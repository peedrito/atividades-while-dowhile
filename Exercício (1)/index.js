// Variavel do input do user
const input = 10;
// Variavel para a soma
let soma = 0;
// Sistema de loop
for (let i = 0; i <= input;i++) {
    soma += i;
}
// Imprime no console
console.log(`O valor da soma do input "${input} resulta em "${soma}."`);